#include <stdio.h>

/**
 * main - Program prints the lowercase alphabet in reverse
 *
 * Return lways 0 (Success)
 */

int main(void)
{
	int ch;

	for (ch = 'z'; ch <= 'z'; ch--)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
