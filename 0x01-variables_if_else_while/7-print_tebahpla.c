#include <stdio.h>

/**
 * main - Program prints the lowercase alphabet in reverse
 *
 * Return lways 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 10; num != 0; num--)
	{
		putchar('num');
	}
	putchar('\n');
	return (0);
}
