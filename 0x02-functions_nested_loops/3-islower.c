#include "main.h"

/**
 * main - Program checks for lower case character
 *
 * Return: i for lower case and 0 if other
 */

int is_slower(int a)
{
	if (a >= 97 && a <= 122)
	{
		return (1);
	}
	else
		return (0);
}
